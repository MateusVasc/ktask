spring.application.name=ktask

# DB
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}

spring.jpa.hibernate.ddl-auto=none

# DB FLYWAY
spring.flyway.enabled=true
